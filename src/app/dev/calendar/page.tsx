'use client';
// CalendarPage 전체 폭 시연 — 타임라인은 폭이 필요해 박물관 카드보다 여기서 본다.
// 데모 데이터/인코딩은 _registry(시공 도메인은 dev에만)에서 재사용 — 중복 0.
import { Demo } from '@/ui/_dev';

export default function CalendarDev() {
  return <Demo name="CalendarPage" />;
}
